// Explicit diagnostic harness. No dependency or production-source changes.
gradle.beforeProject { p ->
    if (p.path != ':app') return
    p.plugins.withId('com.android.application') {
        p.android.sourceSets.getByName('androidTest').kotlin.srcDir(
            p.rootProject.file('engineering/device-diagnostics')
        )
    }
}
